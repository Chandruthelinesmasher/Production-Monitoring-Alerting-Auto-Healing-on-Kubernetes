apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: sre-app-monitor
  labels:
    app: sre-app
    release: monitoring
spec:
  selector:
    matchLabels:
      app: sre-app
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s